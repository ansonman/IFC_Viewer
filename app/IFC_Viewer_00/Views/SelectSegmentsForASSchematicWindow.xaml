<Window x:Class="IFC_Viewer_00.Views.SelectSegmentsForASSchematicWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="AS原理圖：選擇兩段管件" Height="240" Width="520"
        WindowStartupLocation="CenterOwner">
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="請在 3D 視圖選取兩段管件（依序：第 1 段 → 第 2 段）。"/>

        <StackPanel Grid.Row="1" Orientation="Vertical" Margin="0,8,0,8">
            <StackPanel Orientation="Horizontal" Margin="0,4,0,0">
                <TextBlock Text="第 1 段：" Width="60"/>
                <TextBlock Text="{Binding Seg1Display}"/>
            </StackPanel>
            <StackPanel Orientation="Horizontal" Margin="0,4,0,0">
                <TextBlock Text="第 2 段：" Width="60"/>
                <TextBlock Text="{Binding Seg2Display}"/>
            </StackPanel>
        </StackPanel>

        <TextBlock Grid.Row="2" TextWrapping="Wrap" Foreground="#666">
            小提示：若選錯，可按「重置選擇」後重新挑選。完成兩段後會自動計算參考平面並產生點位原理圖。
        </TextBlock>

        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="重置選擇" Margin="0,0,8,0" Padding="10,4" Command="{Binding ResetCommand}"/>
            <Button Content="關閉" Padding="10,4" IsCancel="True"/>
        </StackPanel>
    </Grid>
</Window>
